[build]
  command = "npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Keep Supabase alive - runs every 3 days (at 8:00 AM)
[functions."keep-alive"]
  schedule = "0 8 */3 * *"
